import { render, screen } from "@testing-library/react";
import "@testing-library/jest-dom";
import React from "react";
import Greeting from "./Greeting";
import 
describe("Greeting", () => {
  it("renders correctly", () => {
    user = 
    render(<Greeting currentUser={} />);
  })
})
